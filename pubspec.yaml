name: hello_world_dapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Dépendances pour la blockchain
  provider: ^6.1.2
  web3dart: ^2.7.3
  http: ^1.2.2
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Asset pour le fichier JSON du contrat
  assets:
    - src/artifacts/HelloWorld.json